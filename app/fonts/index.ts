import { Inter } from 'next/font/google'

// Predefined inter fonts, from google with fallback and being optimized
export const interFont = Inter({
  display: 'swap',
  variable: '--font-inter',
  subsets: ['latin'],
})
